# Setup the Ansible control node to run the playbooks

- Install Ansible on the control node.
  - pipx is recommended for installing Ansible:

    ```bash
    pipx install ansible
    ```

  - pipx inject ansible bcrypt
